version: '3.3'
services:
  postgres:
    container_name: postgres
    image: postgres:9.6.1
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: user
